const capitalize = (string) => {
  if (typeof s !== 'string') return '';
  return string.charAt(0).toUpperCase() + string.slice(1)
}

$("modal_edit").click(function(){
  $("modal_edit").toggleClass("is-active");
});
